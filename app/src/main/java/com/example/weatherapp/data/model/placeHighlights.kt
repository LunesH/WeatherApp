package com.example.weatherapp.data.model

/**
 * placeHighlights data class for autocompletion.
 * */
data class placeHighlights(
    var title : List<highlightsPlace>,
    var address : highlightsAddress,
)
